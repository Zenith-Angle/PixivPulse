# Changelog

本项目的公开版本变更记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.5.10] - 2026-09-08

### Fixed

- 公开进展说明、实际工具操作、最终回答独立展示：进展文字随模型响应显示，工具详情默认折叠，完成后收起整个过程并可重新展开。适用于普通查询和多步分析，不绑定采样题目或固定话术。
- 支持 Responses 与 Chat Completions 的公开进展参数增量；普通文本也实时显示，待实际工具调用确认后归入过程，避免提前折叠或等待整段后才显示。复制与后续上下文只使用最终回答。
- 移除选样 4 篇、单次批量 3 篇和每问 3 篇次的固定限制，按用户要求和实际可用上下文读取多篇小说。
- 正文自动档不设额外字数或比例上限，自定义支持更大字数和 100% 覆盖；底层采样及旧页面兼容链路同步更新。
- 输入预算、累计预算、工具轮数支持 0（不设额外上限），新增一键宽松设置，保留已有连接和密钥；无新证据时自动终止重复工具循环。
- 批量采样展示逐篇队列、等待请求间隔、采样字符数、覆盖率和缓存命中，逐篇完成即可看到进度；停止及刷新中断时保留已完成项。
- 网站请求间隔、权限校验、冷却及模型实际上下文限制继续有效。

## [0.5.9] - 2026-09-08

### Fixed

- Responses 文本完成、内容块完成及消息完成事件及时交付文本，按块补齐缺失增量并去重，不再仅等整轮完成兜底。
- 工具调用前的模型说明归入执行记录，最终回答、复制及后续对话上下文不再混入执行说明。
- 执行面板位于回答上方，运行时展开，展示实际查询、逐篇阅读与缓存复用；回答输出时自动收起，支持小方形按钮展开及历史恢复。
- 大块文本采用最多约 600ms 的有界显示缓冲；普通增量立即显示，停止时保留所有已收到的文本，尊重减少动态效果设置。

## [0.5.8] - 2026-09-07

### Fixed

- Agent 样式随看板入口加载，避免已打开看板在更新后懒加载旧 CSS 路径失败。
- Agent 模块加载失败时显示明确的刷新恢复入口，避免未捕获异常导致整个看板失效；不自动刷新或清除数据。
- 安装验证增加构建 JS/CSS 引用完整性及 Agent 样式随入口加载检查。

## [0.5.7] - 2026-09-07

### Fixed

- 正文直读使用 `/ajax/novel/{id}` 的结构化 `content`，校验作品身份、错误信封及字段，移除失效的初始 HTML 解析依赖；不新建标签页，也不在权限错误后切换通道。
- 按账号记住上次选择的对话，重新进入 Agent 时恢复会话并滚动到底部；页面刷新后也恢复选择，后台会话完成不覆盖当前选择。
- 阅读错误不再被解释成作品未开放正文，区分解析/接口兼容性问题与明确的 HTTP 权限错误。

## [0.5.6] - 2026-09-07

### Fixed

- 输入框展开按钮定位到右上角，不再独占一行；折叠状态收紧为两行输入高度。
- 移除桌面和移动端固定高度占位，改为按实际内容布局；展开仍向上延伸，底部操作栏位置不变。

## [0.5.5] - 2026-09-07

### Fixed

- 展开、复制、编辑等消息操作改为小方形图标按钮，保留悬浮说明与无障碍名称；分别定义普通、悬浮、按下、禁用、键盘聚焦状态，修复主按钮悬浮灰底白字。
- 页面验证检测不再扫描整个 HTML 中的验证码相关文案，避免正常脚本或翻译文本误触发；仍拒绝真实挑战页面及无可靠正文的页面。
- 正文首次失败后关闭本问工具并说明原始错误，停止重复读取及所谓备用通道；明确应用冷却不是新的 Pixiv 限流响应，不建议通过新会话绕过限制。

## [0.5.4] - 2026-09-07

### Changed

- 展示模型连接、处理问题、准备工具、读取正文、复用证据和输出回答的真实阶段及累计用时，保留有界过程记录；不展示内部推理原文。
- 正文读取复用已有页面，否则直接读取原文 HTML，不创建/关闭标签页；网络请求全局串行并限频，尊重 Retry-After，权限错误和页面结构异常暂停后续直接读取。
- 输入框取消原生拖动缩放，改为底边固定、向上展开/收起。

### Fixed

- 修复 40ms 绘制节流窗口内的最后一批文本可能一直等待下一块或最终完成才显示的问题。

## [0.5.3] - 2026-09-07

### Changed

- 新配置默认单次输入预算 64,000、单问累计预算 160,000；已有配置保留，可用推荐预算按钮更新。
- 正文深度提供轻量（1,500 字 / 30%）、标准（3,000 字 / 50%）、深入（6,000 字 / 70%）及自定义；自定义支持 150–6,000 字、10–70%，实际受剩余预算约束。
- 默认回答偏好加入可编辑的 Pixiv 同人小说示例；系统提示要求用已确认标题、系列和章节称呼作品，仅在无标题或消歧时使用编号。

## [0.5.2] - 2026-09-07

### Changed

- 每条消息提供复制和重新编辑；回答支持引用追问、导出本轮、从此处重试。重试创建分支并保留原对话及后续记录。

- 增加自动/正文优先/指标优先侧重，正文问题省略自动统计总览，缩减工具集合及重复统计字段，保留来源、未知值和实际观测端点。
- 支持多篇正文合并阅读，按系列、独立作品及篇幅差异选样；每问每篇累计标准阅读上限 3,000 字 / 50%，深入 6,000 字 / 70%，仍受输入预算及每问 3 篇次约束。
- 正文模式为正文证据预留最多单次预算的 70%，统计证据限制约 2.4 KB；重复查询引用已有来源，展示正文/统计工具证据字节量。
- 预设扩展到最多 9 类 / 32 问，先区分文体，再依据正文分析角色、叙事机制和创作决策；要求反例、替代解释及小规模验证，避免将个人投稿过拟合到开源默认值。

### Fixed

- 兼容长篇正文区域与外层 main 并列的 Pixiv 页面布局，仍排除简介、推荐及隐藏段落；对歧义阅读区拒绝猜测。
- 校验侧重/阅读深度配置，缓存键包含采样版本和预算，避免复用旧的低覆盖片段。

## [0.5.1] - 2026-09-07

### Fixed

- 新建/切换对话立即离开连接配置；生成状态、停止控制、草稿按会话隔离，后台结果不会抢走当前选择，最多同时生成 3 个对话。
- 粉丝查询同时给出起止粉丝数与实际时间，明确记录数含义，避免模型反复查询末端数据。
- 模型在最终回答中输出未执行工具标记时明确报错，不冒充成功。

### Changed

- 输入预算独立于模型窗口，限制单次与单问累计输入，预留总结；自动提供精简概览，超大分页自动缩小并给出续页位置。
- 增加全作品指标/转化排行、系列与内容类型聚合、一站式分析简报，以及按数据能力显示的分类问题。
- 加入可关闭/清除的轻量证据缓存，按数据指纹失效，限制 24 小时、40 条和 256 KB；数据库增量升级保留已有对话。
- 新增按需小说原文采样：开头、中段、结尾、均衡三段或关键词；每次最多 900 字 / 已加载正文的 20%，每问最多 3 次，缓存只保存片段。需要独立开关和数据分享授权。
- 展示请求数、本地记忆命中及服务商缓存输入；新增真实备份与 DeepSeek 效率验收脚本。

## [0.5.0] - 2026-09-07

### Added

- 新增 Agent 分析工作区：多会话、连续追问、流式 Markdown/表格、停止与重试、对话导出、可展开的工具来源。
- 支持 DeepSeek 官方 Responses 预设、自定义 OpenAI Responses / Chat Completions 接口、模型、密钥、上下文和输出预算、工具轮数、超时与回答偏好。
- 加入 7 个只读分析工具和内存知识索引，按需查询作品、增长、粉丝及数据质量，保留未知值、负增长和实际采样时间。
- 独立存储 Agent 配置与按账号隔离的对话；密钥默认仅保留在当前标签页，远程域名按需授权，作品备份不包含密钥。
- 新增真实协议调用链、数据检索、存储隔离、异常处理及聊天界面的验收测试与本地浏览器验收服务。
- 增加真实 DeepSeek 验收脚本；适配思考模式的自动工具选择，提供带北京时间偏移的查询时间，明确作品样本数和采集轮次的区别。

### Changed

- 更新隐私说明，明确开启 Agent 数据分享后查询结果会发送至用户选择的服务。
- 版本验收改为通用三段式版本检查，允许功能版本正常升级至 0.5.x。

## [0.4.20] - 2026-09-07

### Fixed

- 比较页所有时间范围统一显示缩放拖动条，修复“今日”和“近 3 天”因分桶数恰好不超过 24 而改变图表布局的问题。

### Changed

- 将缩放条与图表底部布局收口到同一配置，删除未接入主流程的旧相对百分比比较图、无调用别名和转发组件。

## [0.4.19] - 2026-09-07

### Changed

- 比较页增量改为共享时间分段内记录到的净变化，24 小时、3 天、7 天、30 天、3 个月分别按 1 小时、3 小时、6 小时、1 天、3 天合并。
- 悬停沿用原生比较提示框样式，显示分段起止时间与净变化；无观察时段留空，并保留负值和未满分段。

## [0.4.18] - 2026-09-05

### Fixed

- 曲线改为按真实时间间隔进行局部加权的平滑趋势，减少重复计数后突然跳升造成的阶梯感；缩放时按可见时间跨度重新计算。
- 趋势绘制与原始采样分离，悬浮框仍显示真实观察值并按数值降序排列，不将平滑估算写入历史数据。
- 保留趋势首末值，并区分平滑趋势与采样值；粉丝缩略图使用相同平滑方式。
- 修复缩放交互与坐标轴替换同时发生时读取未初始化 `scale` 的运行异常。

## [0.4.17] - 2026-09-05

### Fixed

- 调整总览、作品、粉丝及比较曲线的平滑方式，改善稀疏采样时的阶梯感，保留原始观察点与数值。
- 比较图悬浮提示按当前指标数值降序排列，适用于总数、增量和相对变化。

## [0.4.16] - 2026-09-04

### Added

- 首次公开发布 PixivPulse 源码与可校验的 Chrome MV3 扩展包。
- 提供完整的项目首页、安装说明、隐私边界、开发流程与演示截图。

### Changed

- 将本地浏览器数据导出脚本的默认路径改为从当前 Windows 环境推导，移除开发机器专属路径。
- 将备份目录提示改为与 Windows 用户名无关的通用说明。

[0.4.16]: https://github.com/Zenith-Angle/PixivPulse/releases/tag/v0.4.16
