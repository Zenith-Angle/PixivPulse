# PixivPulse 隐私政策

生效及更新日期：2026 年 9 月 8 日。适用于 PixivPulse 0.5.11 的现有功能。

PixivPulse 由 GitHub 开发者 Zenith-Angle 维护，是独立的 Pixiv 作者作品增长分析扩展，与 Pixiv 官方无隶属关系。本政策说明扩展如何处理数据，包括仅保存在您设备上的数据。

## 处理哪些数据

| 数据 | 用途 |
| --- | --- |
| Pixiv 账号 ID、昵称及账号关联信息 | 区分账号、关联作品和历史记录 |
| 作品 ID、标题、链接、类型、系列、封面和作品元数据 | 展示作品库、搜索、筛选和比较 |
| 浏览、收藏、获赞、评论数量等作品指标，以及作者关注者数量和采样时间 | 生成历史快照、趋势、增量和分析结果；不等同于读取私信或评论正文 |
| 同步设置、任务状态、运行错误与界面偏好 | 执行和恢复同步任务、排查问题、保存使用设置 |
| 可选 AI 功能的对话、分析指令、模型设置和分析记录 | 提供用户主动发起的作品分析与对话 |
| 用户填写的 AI API Key | 向用户指定的模型服务进行身份验证 |
| 用户开启正文采样后读取的小说内容 | 支持作品内容分析，并在启用记忆时缓存分析所需资料 |

扩展不设广告追踪或开发者遥测服务，不主动采集其他网站的浏览历史、鼠标轨迹、按键记录、位置、健康或付款信息。请勿在 AI 对话、问题反馈或导出文件中加入与作品分析无关的敏感信息。

## 本地存储与网络请求

作品历史、设置、缓存和 AI 对话主要保存在当前浏览器的扩展存储及 IndexedDB 中。开发者不运营接收这些作品数据或对话的自有后台。

同步功能使用浏览器现有的 Pixiv 登录会话访问 www.pixiv.net；浏览器可能随请求发送该站点的认证 Cookie。扩展不要求填写 Pixiv 密码，也不会将 Pixiv Cookie 或密码发送到 AI 服务。作品封面通过 i.pximg.net 获取。

为显示封面，扩展只为自身发往 i.pximg.net 的封面下载请求设置 Pixiv Referer，不修改其他网站的普通浏览流量。

## 可选 AI 分析与第三方服务

图表、作品库、比较和同步不要求连接 AI 服务。用户配置服务并主动发送问题后，对话上下文、分析指令和相应 API 请求将发送到所选服务；即使没有开启本地数据分享，您主动输入的对话仍会发送。

“分享本地数据”和“正文采样”默认关闭。开启本地数据分享后，作品指标和相关元数据可作为分析上下文发送；同时开启正文采样后，分析所需的小说正文内容也可发送。扩展只调用安装包中实现的分析工具，不向模型提供任意执行代码的能力。

API Key 作为认证凭据发送到用户配置的服务地址，不作为对话正文发送。远程 AI 地址必须使用 HTTPS；HTTP 仅允许 localhost、127.0.0.1 和 IPv6 回环地址的本机服务。扩展按用户选择的来源请求可选访问权限。

Pixiv、图片服务、用户选择的 AI 服务及网络服务商会接收处理请求所需的网络信息（例如 IP 地址）。第三方可能依其自身条款保存请求、对话或日志；数据保留、模型训练使用及删除方式取决于所选服务。PixivPulse 不保证任意第三方服务均不留存或不用于训练，请在连接前核对该服务的政策和账户设置。

## 数据用途与共享限制

开发者不出售用户数据，不将数据用于广告、与作品增长和内容分析无关的用途、信用评估或贷款决策。数据传输仅用于用户请求的上述功能，或用户主动进行的导出和反馈。

PixivPulse 对用户数据的使用遵守 Chrome Web Store 用户数据政策，包括 Limited Use（限制使用）要求。第三方 AI 数据处理应限于用户请求的分析服务；请不要连接将这些数据用于无关用途的服务。

## 保留、删除与导出

本地作品历史会按扩展的保留与抽稀设置管理；有损历史整理需要满足扩展的备份和校验条件。封面及分析缓存可随缓存维护而更新。AI 对话保存在独立数据库中，清理作品历史不会同时删除 AI 对话。

您可以使用扩展提供的备份、对话导出、对话删除和分析记忆清理功能管理相应记录。关闭本地数据分享或正文采样可阻止后续分析使用相应数据，但不会撤回此前已发送给第三方的内容。您也可以在浏览器扩展设置中撤销权限，或卸载扩展以移除其浏览器本地存储。

导出到磁盘的 JSON、CSV、Markdown 及自动备份文件不会因卸载而自动删除，需要您自行管理。已发送到第三方的数据需要通过对应服务申请删除。

## 安全与 API Key

API Key 默认仅保存在当前标签页会话中。选择“记住”后，Key 会保存在本机扩展数据库中，**没有应用层加密**；关闭记住并保存配置会移除持久化配置中的 Key。作品备份及对话 Markdown 导出不包含 API Key。

本地数据库和导出文件也不提供应用层加密，文件校验值仅用于完整性检查。请保护设备和浏览器账户，妥善保管备份，避免向他人提供包含隐私的数据。若 Key 泄露，应在模型服务端撤销或更换。

## 联系与政策变更

维护者：Zenith-Angle。隐私相关问题可通过 [项目 Issues](https://github.com/Zenith-Angle/PixivPulse/issues) 联系。Issues 是公开页面，请仅提交不含敏感数据的问题说明，不要上传 API Key、Cookie、个人完整备份或私密正文。

本政策将随实际功能变化更新，并在本页标注更新日期。
